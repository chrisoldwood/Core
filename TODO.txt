- Handle type for sharing.

- IOStream for tracing out.

- Nullable<T>

- Switch to c++ headers e.g. <cstdarg> instead of <stdarg.h>

- Add non-MS impl of Trace() & AssertFail().

- Add overloads of parse that takes a const tchar* instead of tstring.

- Command line parser does not line-up 2 character short names.
